# graph point info

uint16 id                       # id of the point
GraphPointInfo[] predecessors   # points that can be reached from this point
GraphPointInfo[] successors     # points that can reach this point